<div class="row justify-content-center mt-3">
  <div class="col col-md-6">

    <h2><%= t('.header', rule: @badge_rule.rule) %></h2>

    <%= render 'form' %>
    <%= link_to t('.back'), admin_badge_rules_path, class: 'btn btn-primary' %>
  </div>
</div>
